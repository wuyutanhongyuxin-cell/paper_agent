# Changelog

## [0.1.0] - unreleased

### Added
- `paper-agent` CLI (`init` / `audit` / `compile` / `apply`)
- 3 个 audit 规则模块：`bib_audit` / `punct_audit` / `humanize_check`
- Edit Gate 三层网关（TTY 信任锚 + flock + patch --strict）
- latexmk 替代 4-pass 手写编译
- 失语症 paper 端到端 0 回归
