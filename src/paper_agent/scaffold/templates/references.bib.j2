% references.bib — scaffold from `paper-agent new`
%
% This file ships with ONE placeholder @misc entry so the first
% latexmk run is fully green (no biber "Empty database" warning;
% no biblatex "no bibliography data" error). Replace it before
% writing real citations.
%
% Useful commands:
%   paper-agent audit <paper_root>      # bib_audit will flag any TODO_<key>
%   paper-agent compile <paper_root>    # latexmk + pre-flight audit gate

@misc{example_remove_me,
  author       = {Example Author},
  title        = {A placeholder reference. Replace this entry with a real citation},
  year         = {2026},
  howpublished = {Generated by paper-agent scaffold},
  note         = {Remove this entry after the first real reference is added},
}
